{
  "name": "roughrunway-mcp",
  "version": "1.0.0",
  "description": "MCP server for RoughRunway — build crypto treasury runway models from Claude Desktop",
  "keywords": ["mcp", "crypto", "treasury", "runway", "defi", "claude"],
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "roughrunway-mcp": "dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18"
  }
}
